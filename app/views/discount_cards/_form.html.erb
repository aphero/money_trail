<%= form_for(discount_card) do |f| %>
  <% if discount_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(discount_card.errors.count, "error") %> prohibited this discount_card from being saved:</h2>

      <ul>
      <% discount_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :store_id %>
    <%= f.number_field :store_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :category_id %>
    <%= f.number_field :category_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
